import 'package:flutter/material.dart';

class ZyoubunContent extends StatelessWidget {
  const ZyoubunContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        '条文階層画面',
        style: TextStyle(color: Colors.white, fontSize: 24),
      ),
    );
  }
}
